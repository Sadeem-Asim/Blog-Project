<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Compose</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      input {
        width: 100%;
        margin-bottom: 10px;
      }
      input.title {
        height: 25px;
        border: 1px solid rgba(0, 0, 0, 0.603);
        padding: 15px;
        outline: none;
      }
      textarea {
        height: 160px;
        border: 1px solid rgba(0, 0, 0, 0.603);
        padding: 15px;
        padding-bottom: 82px;
        outline: none;
        width: 100%;
      }

      h6 {
        font-size: 20px;
        margin: 10px 0 10px 0;
      }
      button {
        margin-top: 10px;
        padding: 10px 10px;
        text-transform: uppercase;
        background-color: aqua;
        cursor: pointer;
        border: 1px solid transparent;
        border-radius: 10px;
      }
      form {
        max-width: 1000px;
        margin: 0 auto;
      }
    </style>
  </head>
  <body>
    <%- include('header'); -%>
    <form action="/compose" method="post">
      <h1>Compose</h1>
      <h6>Title</h6>
      <input type="text" name="postTitle" class="title" />
      <h6>Content</h6>
      <textarea
        name="postContent"
        cols="30"
        rows="10"
        placeholder="textHere"
      ></textarea>
      <button type="submit">Publish</button>
    </form>
    <%- include('footer'); -%>
  </body>
</html>
